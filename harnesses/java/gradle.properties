# The tracked variable for scenario S001: the OpenTelemetry API artifact
# version. Declared here (not as a build.gradle `def`) so Renovate's Gradle
# manager detects and bumps it automatically; a merged bump triggers a new
# benchmark run. CI can override it with -PotelApiVersion=X.Y.Z.
otelApiVersion=1.51.0
